# PRD: GGD Autonomy Production Horizon

## Status

- date: `2026-04-03`
- owner: `orchestrator`
- state: `active engineering program`
- scope: `/Users/zer0palab/Zer0pa Website/Website-main` and `/Users/zer0palab/Zer0pa Website/worktrees/*`

## Problem

The system is no longer missing doctrine. It is missing alignment and proof.

What is already true:

- `CLAW` can materialize bounded cycles, hold lane locks, settle queues, and fail closed
- `GGD` now governs route gaps and can block downstream integration promotion
- homepage and `/imc` are canonically closed
- the shared product-family kernel exists

What is not yet true:

- all active lane worktrees do not yet reliably carry the same deterministic geometry contract
- the long-horizon plan, runtime state, and press-go manifest do not yet point to one authoritative program
- `systems-qa` and `integration` do not yet treat geometry-law verification as first-class acceptance evidence
- `/work/xr` remains blocked by an open geometry gap
- no full supervised recursive proof has been rerun under the new GGD-governed contract

## Goal

Move from the current guarded but partially fragmented system to a truthfully eligible autonomous local claw system that:

- treats route design as deterministic geometry, algebra, proportionality, and falsification
- improves or holds quality under recursive execution
- refuses promotion when route gaps remain open
- propagates the same contract across all active lane worktrees
- proves the generic product family on `/work/xr` and `/work/ft`
- becomes eligible for guarded unattended operation on this Mac without cross-repo risk

## Non-Goals

- production deployment
- remote pushes or PR creation
- Telegram handoff in this phase
- speculative redesign that is not law-backed
- adding new creative visual languages outside the declared geometry program

## Deterministic Law

The system law remains:

`route_output = f(geometry_constants, route_role, packet_truth, viewport, verification_constraints)`

The autonomy law is:

`promotion = f(route_gap_state, verification_bundles, queue_truth, replay_integrity, recovery_integrity)`

No route is promotable when:

- a blocking GGD gap remains open
- geometry-law evidence is absent for the route family
- the candidate cannot survive build, parser, semantic, responsive, and route-law falsification
- the lane worktree lacks the current contract surface

## Current Truth

- canonical routes:
  - `/`
  - `/imc`
- active generic proof route:
  - `/work/xr`
- replication proof route:
  - `/work/ft`
- current hard blocker:
  - `/work/xr` still has an open geometry gap and remains blocked in GGD

## Program Spine

This PRD becomes the narrative authority for the next engineering horizon.

The authoritative machine plan is:

- `CLAW/control-plane/plans/ggd-autonomy-production-horizon.json`

The following remain appendices, not primary authorities:

- `CLAW/control-plane/plans/autonomous-go-live-hardening.json`
- `CLAW/control-plane/plans/canonical-routes-to-producing.json`
- `CLAW/control-plane/plans/gpd-geometry-adaptation.json`
- `CLAW/control-plane/plans/24x7-hardening-plan.json`

## Phases

### H0. Truth Authority Collapse

Purpose:

- eliminate plan and evidence drift

Tasks:

- make runtime state, press-go manifest, and active PRD point to the same program
- demote stale plans from primary authority
- distinguish current route blockers from historical checkpoint truth
- make the next actions list reflect actual route and runner work

Exit gates:

- runtime state, GGD state, and press-go manifest tell the same story
- no stale primary plan remains referenced as authoritative

### H1. Contract Propagation And Lane Convergence

Purpose:

- ensure active lane worktrees carry the same deterministic geometry contract

Tasks:

- propagate current GGD/CLAW control commits into active lane branches
- verify each relevant worktree exposes the geometry-law audit command surface
- verify each relevant worktree carries the current route-gate logic and GGD files

Exit gates:

- all active execution lanes carry the current contract
- worktree contract verification passes

### H2. Geometry-Law Execution As First-Class QA

Purpose:

- stop treating geometry-law as an optional side report

Tasks:

- make `systems-qa` run geometry-law verification as part of bounded route falsification
- make `integration` require geometry-law evidence for route-family promotion
- add geometry-law artifacts to squads, plans, and salvage rules

Exit gates:

- geometry-law reports are required artifacts for route-family proof
- `systems-qa` and `integration` recipes explicitly honor geometry-law evidence

### H3. XR Closure Under Shared Kernel Law

Purpose:

- close `/work/xr` without bespoke route hacks

Tasks:

- refresh XR truth and latest route evidence
- fix only shared product-family law surfaces
- close the blocking XR gap through `systems-qa`

Exit gates:

- `/work/xr` has zero critical and zero major layout diffs
- `/work/xr` has no blocking geometry-law or route-gap failure
- no XR-only fork is introduced

### H4. FT Replication Proof

Purpose:

- prove the same family law generalizes to a second repo-backed page

Tasks:

- apply the shared kernel to `/work/ft`
- run truth, layout, geometry-law, semantic, and responsive falsification
- create an explicit FT gap if it fails

Exit gates:

- `/work/ft` closes cleanly or produces an explicit deterministic gap record
- XR and FT are both explainable under one family law

### H5. Integration Currency And Checkpoint Freshness

Purpose:

- make integration evidence current again

Tasks:

- replay accepted XR and FT candidates through `integration`
- create a new integration checkpoint from current accepted commits
- stop relying on older checkpoint evidence as if it were current

Exit gates:

- latest checkpoint matches latest accepted route-family commits
- integration evidence is current for XR and FT

### H6. Matrix And Augmentation Law Expansion

Purpose:

- expand beyond the minimum viewport set and seed lawful augmentation

Tasks:

- extend the viewport matrix to larger displays and limiting cases
- keep augmentation items law-backed and measurable
- preserve canonical and route-family integrity under the expanded matrix

Exit gates:

- responsive matrix is current and truthful
- augmentation program is seeded as measurable law backlog

### H7. Supervised Recursive Proof

Purpose:

- prove the new system can recurse without manual surgery

Tasks:

- run a supervised `C4 -> C5` style loop under the current GGD-governed contract
- require complete settlement, reports, gaps, and queue truth
- reject any loop that depends on manual queue editing

Exit gates:

- supervised cycle passes
- no stale lock or stale runner mismatch survives

### H8. Recovery And Interruption Proof

Purpose:

- prove the actual failure paths of the current runner

Tasks:

- exercise stale-job recovery, replay conflict settlement, interruption recovery, and runner fatal settlement
- capture fresh evidence for the live runner contract

Exit gates:

- recovery evidence is current for the current autonomy runner
- no unresolved recovery drift remains

### H9. Guarded Unattended Eligibility

Purpose:

- become truthfully eligible for bounded unattended local autonomy

Tasks:

- run a bounded guarded local window on this Mac
- verify improvement-or-hold behavior only
- confirm no path-scope, queue, or evidence drift

Exit gates:

- guarded local autonomy passes
- `press_go` remains false until human authorization
- the system is truthfully eligible rather than merely operational

## Streams

- `orchestrator`: control truth, sequencing, evidence, settlement
- `data-truth`: packet truth and GitHub-backed route data
- `product-family`: shared route-family law application
- `systems-qa`: falsification, rejection, geometry-law enforcement
- `integration`: replay rehearsal, checkpoint freshness, route-family promotion proof
- extra-reasoning reviewers: plan critique, geometry critique, and readiness critique

## Immediate Execution Order

1. Collapse planning truth into one active PRD/plan spine.
2. Propagate the contract to active lane worktrees.
3. Promote geometry-law verification into lane execution rules.
4. Prove worktree contract readiness.
5. Resume XR closure with the stronger contract.

## Readiness Rule

The system is not ready for unattended recursive work until:

- active lane worktrees carry the same current deterministic contract
- XR and FT are closed or explicitly gapped under the shared family law
- integration evidence is current
- supervised recursion has passed under the current runner
- recovery evidence is current
- guarded autonomy has been proven locally

`press_go` remains `false` until those conditions are satisfied and explicit human authorization is recorded.

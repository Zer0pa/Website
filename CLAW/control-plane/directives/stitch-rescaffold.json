{
  "version": 1,
  "directive_id": "STITCH-RESCAFFOLD-20260406",
  "created_at": "2026-04-06T14:00:00Z",
  "created_by": "operator",
  "priority": "critical",
  "title": "Rescaffold product pages from Google Stitch HTML prototypes",
  "summary": "Three Google Stitch HTML prototypes are available on the local filesystem. These contain PRODUCTION-READY HTML with Tailwind CSS. They are NOT design references to reinterpret — they are CODE SCAFFOLDS to use as literal starting points. The agent MUST copy the Stitch HTML structure into the Next.js components, preserving the exact Tailwind classes, section ordering, and markup hierarchy. The project currently has NO Tailwind — agent must install it (npm install -D tailwindcss @tailwindcss/postcss postcss) and configure it before the Stitch classes will work. Do NOT reimplement the Stitch layouts in custom CSS. Do NOT 'extract patterns' and rebuild. COPY the Stitch HTML, adapt it minimally for Next.js (JSX syntax, next/font, next/image, dynamic content wiring), and keep the Tailwind classes intact.",
  "stitch_sources": {
    "homepage": {
      "path": "/Users/zer0palab/Zer0pa Website/Zer0pa Homepage/",
      "description": "Landing page / homepage design. Separate design from product pages.",
      "target_route": "/"
    },
    "product_page_template": {
      "path": "/Users/zer0palab/Zer0pa Website/Zer0pa ZPE Product Pages/",
      "description": "Generic product page template for individual ZPE repos (XR, FT, Geo, Ink, IoT, Mocap, Neuro, Prosody, Robotics). Each product page draws ALL content from its GitHub repo. This is an enhanced, visualized GitHub page with placeholder spaces for physics widgets/visualizations of the 8 primitives. DARPA-leak aesthetic.",
      "target_routes": ["/work/xr", "/work/ft", "/work/geo", "/work/ink", "/work/iot", "/work/mocap", "/work/neuro", "/work/prosody", "/work/robotics"]
    },
    "zpe_imc_page": {
      "path": "/Users/zer0palab/Zer0pa Website/Zer0pa ZPE-IMC Page/",
      "description": "ZPE-IMC flagship page. HYBRID role: (1) its own product story as a codec, AND (2) overarching foundational content/modelling/explanation/context for the repos that sit under it. Must merge the product page template with the IMC-specific design.",
      "target_route": "/imc"
    }
  },
  "content_pipeline": {
    "principle": "Every product page pulls its content and copy DIRECTLY from the relevant GitHub repo. The Stitch HTML is conceptual layout only — no content guidance was given to Stitch. The agents must wire up the packet cache (site/.cache/packets/) and loadLaneCatalog()/loadLaneBySlug() to populate the Stitch layout shells with real repo data.",
    "visuals": "Use placeholder visuals/boxes for now in spaces where visualizations/widgets are needed. These will be filled later with physics visualizations of the 8 primitives.",
    "merchandising": "There is thinking still to do about how to merchandise content from repos onto pages. For now, map obvious sections (hero, metrics, proof assertions, evidence routes, related lanes) to packet fields and GitHub data."
  },
  "agent_instructions": {
    "product-family": "STEP 1: Install Tailwind CSS — run `npm install -D tailwindcss @tailwindcss/postcss postcss` in site/ and create tailwind.config.ts pointing at src/**/*.{ts,tsx}. Add @tailwind directives to globals.css (keep existing custom CSS below them). STEP 2: Read the Stitch product page HTML file (code.html). STEP 3: COPY the Stitch HTML structure directly into /work/[slug]/page.tsx as JSX. Keep ALL Tailwind utility classes exactly as they appear in the Stitch code. Do NOT rewrite them as custom CSS. Do NOT 'extract patterns'. The Stitch HTML IS the code — adapt it minimally: convert HTML attributes to JSX (class→className, for→htmlFor), replace <img> with next/image, replace Google Fonts links with next/font/google, replace static text with dynamic packet data from loadLaneBySlug(). STEP 4: Use placeholder divs with data-slot attributes for visualization spaces. STEP 5: Verify with npm run build.",
    "homepage-fidelity": "Read the Stitch homepage HTML. This is a SEPARATE design from the product pages. Compare against the current homepage implementation and identify structural divergences. The homepage is already ACCEPTED in the CLAW system, so only flag changes — do not break what works.",
    "data-truth": "Verify that packet cache fields can populate all content sections in the Stitch product page template. Identify any gaps where the packet schema needs new fields to support the Stitch layout.",
    "systems-qa": "After product-family delivers a Stitch-based candidate, run falsification using the Stitch HTML as the layout reference rather than the old geometry specs. The Stitch code IS the design spec.",
    "integration": "The Stitch rescaffold is a layout-level change. Verify that the new layout still passes build, parser tests, and does not regress accepted routes."
  },
  "scope_notes": [
    "CRITICAL: The project currently has NO Tailwind installed. The agent MUST install it first: `cd site && npm install -D tailwindcss @tailwindcss/postcss postcss` and create a tailwind.config.ts. The Stitch prototypes use cdn.tailwindcss.com — the Next.js site must use Tailwind via PostCSS. Keep the exact same Tailwind utility classes from the Stitch HTML — do NOT convert them to custom CSS.",
    "Stitch uses Google Fonts links — the Next.js site uses next/font. Preserve the Oswald + Courier Prime choices but use the Next.js font loading pattern.",
    "The Stitch img src uses Google CDN URLs — replace with Next.js Image component and placeholder images.",
    "Do NOT discard GGD geometry laws. The Stitch layout becomes the new geometry reference, and laws should be updated to match it rather than the old abstract specs."
  ],
  "brand_assets": {
    "logo_svg": "/Users/zer0palab/Zer0pa Website/ZER0PA LOGO.svg",
    "icon_svg": "/Users/zer0palab/Zer0pa Website/ZER0PA ICON.svg",
    "usage_notes": "Logo SVG must be used directly (not recreated in code) in all prescribed placements (nav header, footer). Copy the SVG into site/public/ and reference via Next.js. Icon SVG is reserved for future creative phases — do not use yet."
  }
}

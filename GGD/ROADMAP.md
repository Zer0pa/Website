# GGD ROADMAP

## Phase 0. Map Existing Design Truth

Goal:

- map current mockups, route specs, packet truth, verification surfaces, and autonomy evidence

Outputs:

- current route truth summary
- explicit closure status for `home`, `/imc`, `/work/xr`, and `/work/ft`

## Phase 1. Lock Conventions

Goal:

- freeze geometry, typography, color, semantic, and code-quality conventions

Outputs:

- authoritative convention lock
- explicit forbidden moves

## Phase 2. Build The Verification Catalog

Goal:

- define the named verification bundles and the checks inside each one

Outputs:

- geometry bundle
- responsive bundle
- contrast bundle
- semantic and SEO bundle
- truth bundle
- code-quality bundle

## Phase 3. Build The Command And Agent Surface

Goal:

- define the GPD-shaped operational surface for geometry work

Outputs:

- map, discuss, plan, execute, verify, debug, and convention validation flows
- agent-role definitions aligned to route and verification work

## Phase 4. Migrate Active Route Work

Goal:

- move current route closure evidence and failures into GGD state

Outputs:

- canonical closure memory for `home` and `/imc`
- explicit gap records for `/work/xr` and `/work/ft`

## Phase 5. Supervised Route Closure Under GGD

Goal:

- run active route work against the GGD contract instead of parallel doctrine

Outputs:

- accepted route closures that cite GGD conventions and verification bundles

## Phase 6. Guarded Local Autonomy

Goal:

- prove recursive improvement while preserving or improving quality

Outputs:

- supervised cycle evidence
- guarded autonomy evidence
- recovery evidence

## Phase 7. Augmentation Phase

Goal:

- add design augmentations only after canonical closure

Outputs:

- measured augmentation proposals
- flagship and product-family enhancements that do not degrade the kernel
